<?php
/**
* Implementing hook_theme()
*/
function bookModule_theme($existing, $type, $theme, $path) {
  return array(
    'book_list' => array(
      'variables' => array('book' => array(), 'title' => '')
    )
  );
}
